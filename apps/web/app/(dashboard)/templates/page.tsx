/**
 * Templates list page
 *
 * Lists available templates for the org.
 * Even with a single template in MVP, build the picker UI so adding
 * template #2 (P1) is a config change, not a UI change (PRD §16).
 *
 * API: GET /v1/templates
 */
export default function TemplatesPage() {
  return (
    <main>
      <h1>Templates</h1>
      {/* TODO (Epic 5 / P1): implement template picker */}
      <p>Templates page — Epic 5</p>
    </main>
  );
}
