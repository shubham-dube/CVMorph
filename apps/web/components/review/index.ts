/**
 * Shared component stubs — Epic 4
 *
 * Components to build (one file each, kept in this directory):
 *
 *  ConfidenceBadge.tsx  ← done (stub)
 *  EvidencePopover.tsx  ← done (stub)
 *  FieldEditor.tsx      ← TODO Epic 4.4: inline edit/confirm/remove for a single field
 *  BulletList.tsx       ← TODO Epic 4.4: list of reviewable bullets with per-bullet actions
 *  EmploymentSection.tsx← TODO Epic 4.4: full employment entry with collapsible detail
 *  ApproveGate.tsx      ← TODO Epic 4.6: "Approve & Generate" button with disabled state
 */

export { ConfidenceBadge } from "./ConfidenceBadge";
export { EvidencePopover } from "./EvidencePopover";
