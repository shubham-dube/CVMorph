[project]
name = "cv-platform-api"
version = "0.1.0"
description = "CV Transformation Platform — FastAPI backend"
requires-python = ">=3.12"

dependencies = [
    # Web framework
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "python-multipart>=0.0.9",  # file uploads

    # Settings / config
    "pydantic[email]>=2.7.0",
    "pydantic-settings>=2.3.0",

    # Database
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "asyncpg>=0.29.0",          # async postgres driver
    "psycopg2-binary>=2.9.0",   # sync driver for alembic

    # Auth
    "python-jose[cryptography]>=3.3.0",
    "bcrypt>=4.0.0",

    # Async jobs
    "celery>=5.4.0",
    "redis>=5.0.0",

    # Document parsing
    "PyMuPDF>=1.24.0",         # PDF parsing
    "python-docx>=1.1.0",      # DOCX parsing
    "docxtpl>=0.18.0",         # DOCX template rendering

    # AI provider SDK
    "anthropic>=0.30.0",
    "google-genai>=1.0.0",

    # Object storage
    "boto3>=1.34.0",           # S3 (or GCS via compat layer)

    # Observability
    "opentelemetry-sdk>=1.25.0",
    "opentelemetry-instrumentation-fastapi>=0.46b0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "httpx>=0.27.0",           # async test client for FastAPI
    "factory-boy>=3.3.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
